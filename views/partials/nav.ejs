<header class="bg-red-500 text-white py-5 px-4 shadow-md w-full">
  <nav class="max-w-7xl mx-auto px-4 flex flex-col md:flex-row justify-between items-center gap-4">
    <div class="flex items-center space-x-4">
      <h1 class="text-3xl md:text-4xl font-extrabold tracking-tight">Stayora</h1>
      <span class="hidden md:inline-block text-sm bg-red-600/40 px-3 py-1.5 rounded-full font-medium">by Amit Singh Rajput</span>
    </div>
    <ul class="flex flex-wrap justify-center gap-2 md:gap-4 text-base md:text-lg font-medium">
      <li>
        <a href="/" class="<%= currentPage === 'index' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Home</a>
      </li>
      <li>
        <a href="/homes" class="<%= currentPage === 'Home' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Homes</a>
      </li>
      <li>
        <a href="/favourites" class="<%= currentPage === 'favourites' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Favourites</a>
      </li>
      <li>
        <a href="/bookings" class="<%= currentPage === 'bookings' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Bookings</a>
      </li>
      <li>
        <a href="/host/add-home" class="<%= currentPage === 'addHome' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Add Home</a>
      </li>
      <li>
        <a href="/host/host-home-list" class="<%= currentPage === 'host-homes' ? 'bg-red-600' : 'hover:bg-red-600' %> py-2.5 px-4 rounded-md transition duration-300">Host Homes</a>
      </li>
    </ul>
  </nav>
</header>
